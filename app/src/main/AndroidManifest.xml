<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <uses-feature
        android:name="android.software.leanback"
        android:required="false" />

    <uses-feature
        android:name="android.hardware.touchscreen"
        android:required="false" />

    <uses-feature
        android:name="android.hardware.faketouch"
        android:required="false" />

    <application
        android:allowBackup="${appAllowBackup}"
        android:banner="@drawable/drbep_launcher_banner"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="${appFullBackupContent}"
        android:icon="@mipmap/drbep_launcher"
        android:label="@string/app_name"
        android:logo="@drawable/drbep_launcher_banner"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/drbep_launcher"
        android:supportsRtl="true"
        android:usesCleartextTraffic="${appUsesCleartextTraffic}"
        android:theme="@style/Theme.DrbepTvPlayer">

        <activity
            android:name=".MainActivity"
            android:configChanges="screenSize|smallestScreenSize|screenLayout|orientation"
            android:exported="true"
            android:banner="@drawable/drbep_launcher_banner"
            android:icon="@mipmap/drbep_launcher"
            android:label="@string/app_name"
            android:logo="@drawable/drbep_launcher_banner"
            android:supportsPictureInPicture="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <receiver
            android:name=".ReminderActionReceiver"
            android:exported="false" />

        <activity
            android:name=".TvLauncherActivity"
            android:exported="true"
            android:banner="@drawable/drbep_launcher_banner"
            android:icon="@mipmap/drbep_launcher"
            android:label="@string/app_name"
            android:logo="@drawable/drbep_launcher_banner"
            android:roundIcon="@mipmap/drbep_launcher">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LEANBACK_LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>

</manifest>
